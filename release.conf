# Values ship reads. The prose is in RELEASING.md; the loop is in
# ~/notes/releasing.md. Format and keys: anderix/ship, docs/PROFILE.md.
loop=goreleaser
version_file=
apt_package=xql
deb_count=2
crates=
homebrew=yes
winget=Excelano.xql:xql_<version>_windows_amd64.zip
store_lanes=
